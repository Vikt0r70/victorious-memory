---
phase: 02-dashboard-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/app/globals.css
  - apps/web/components/ui/LoadingSpinner.tsx
  - apps/web/components/ui/EmptyState.tsx
  - apps/web/components/ui/ErrorBanner.tsx
  - apps/web/components/layout/Sidebar.tsx
  - apps/web/components/layout/TopBar.tsx
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, ARCH-02]
must_haves:
  truths:
    - "react-force-graph-2d and @tanstack/react-table are installed in apps/web"
    - "shadcn/ui components (button, card, dialog, input, label, select, tabs, switch, checkbox, tooltip, badge, table) exist in apps/web/components/ui/"
    - "LoadingSpinner, EmptyState, and ErrorBanner reusable components exist"
    - "Sidebar navigation links have cursor-pointer and visible hover states"
    - "TopBar has red notification dot on bell icon and focus ring on search input"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Installed dependencies"
      contains: "react-force-graph-2d, @tanstack/react-table"
    - path: "apps/web/components/ui/button.tsx"
      provides: "shadcn/ui Button component"
    - path: "apps/web/components/ui/LoadingSpinner.tsx"
      provides: "Reusable loading spinner"
    - path: "apps/web/components/ui/EmptyState.tsx"
      provides: "Reusable empty state"
    - path: "apps/web/components/ui/ErrorBanner.tsx"
      provides: "Reusable error banner"
    - path: "apps/web/components/layout/Sidebar.tsx"
      provides: "Navigation with hover/cursor fixes"
    - path: "apps/web/components/layout/TopBar.tsx"
      provides: "Top bar with notification dot"
  key_links:
    - from: "apps/web/components/ui/LoadingSpinner.tsx"
      to: "page components"
      via: "import and render"
      pattern: "import.*LoadingSpinner"
    - from: "apps/web/components/ui/ErrorBanner.tsx"
      to: "page components"
      via: "import and render"
      pattern: "import.*ErrorBanner"
    - from: "apps/web/components/ui/EmptyState.tsx"
      to: "page components"
      via: "import and render"
      pattern: "import.*EmptyState"
---

<objective>
Install all new dependencies (react-force-graph-2d, TanStack Table, shadcn/ui) and create the reusable UI component foundation (LoadingSpinner, EmptyState, ErrorBanner) plus global layout fixes (Sidebar hover/cursor, TopBar notification dot).

Purpose: All subsequent page redesigns depend on these components and packages being available.
Output: Installed dependencies, shadcn/ui components in components/ui/, reusable components, refreshed Sidebar and TopBar.
</objective>

<execution_context>
@D:/Projects/Victorious Memory 2V/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Victorious Memory 2V/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-dashboard-redesign/02-CONTEXT.md
@.planning/phases/02-dashboard-redesign/02-RESEARCH.md
@.planning/phases/02-dashboard-redesign/02-UI-SPEC.md
@.planning/phases/02-dashboard-redesign/02-PATTERNS.md
@apps/web/AGENTS.md
@apps/web/app/globals.css
@apps/web/components/layout/Sidebar.tsx
@apps/web/components/layout/TopBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 2-1: Install Core Dependencies and Initialize shadcn/ui</name>
  <files>apps/web/package.json, apps/web/app/globals.css, apps/web/components/ui/button.tsx, apps/web/components/ui/card.tsx, apps/web/components/ui/dialog.tsx, apps/web/components/ui/input.tsx, apps/web/components/ui/label.tsx, apps/web/components/ui/select.tsx, apps/web/components/ui/tabs.tsx, apps/web/components/ui/switch.tsx, apps/web/components/ui/checkbox.tsx, apps/web/components/ui/tooltip.tsx, apps/web/components/ui/badge.tsx, apps/web/components/ui/table.tsx</files>
  <action>
    <!-- Covers CONTEXT.md decisions: D-09, D-10, D-11, D-12 (Component Standardization) -->
    In the `apps/web` directory:
    1. Install base dependencies: `npm install react-force-graph-2d @tanstack/react-table`
    2. Initialize shadcn/ui: `npx shadcn@latest init -d` (accept defaults, Tailwind v4 compatible).
    3. Add shadcn components: `npx shadcn@latest add button card dialog input label select tabs switch checkbox tooltip badge table`
    4. To achieve the "dense information display" (per UX-04 and D-12), update shadcn component files (e.g., `components/ui/table.tsx`, `components/ui/card.tsx`, `components/ui/button.tsx`) to reduce default padding and spacing. Replace `p-4` with `p-2` or `p-3`, use `text-xs` or `text-sm`, and tighten row heights where appropriate.
    5. Ensure color tokens in `globals.css` map to the Material Design 3 dark palette. Do not overwrite existing custom animations or theme tokens.
  </action>
  <verify>
    <automated>
      - `grep -q "react-force-graph-2d" apps/web/package.json`
      - `grep -q "@tanstack/react-table" apps/web/package.json`
      - `test -f apps/web/components/ui/button.tsx`
      - `test -f apps/web/components/ui/table.tsx`
      - `test -f apps/web/components/ui/tabs.tsx`
      - `cd apps/web && npm run build` completes without syntax or module resolution errors
    </automated>
  </verify>
  <done>
    - Theme Integrity: Visually confirm that newly generated shadcn components inherit the existing Material Design 3 dark palette tokens from globals.css (no default Radix UI light-mode colors).
    - Component Density: Inspect `apps/web/components/ui/table.tsx` and confirm padding/spacing utility classes are reduced (e.g., `p-2`, `py-1`, `text-sm` instead of large default padding).
  </done>
</task>

<task type="auto">
  <name>Task 2-2: Create Reusable UI Components and Enhance Layout</name>
  <files>apps/web/components/ui/LoadingSpinner.tsx, apps/web/components/ui/EmptyState.tsx, apps/web/components/ui/ErrorBanner.tsx, apps/web/components/layout/Sidebar.tsx, apps/web/components/layout/TopBar.tsx</files>
  <action>
    <!-- Covers CONTEXT.md decisions: D-18, D-19, D-20, D-21, D-22, D-23, D-24 (Interactive fixes, Design Language, Empty States) -->
    1. Create `apps/web/components/ui/LoadingSpinner.tsx`: Export a centered spinner component using the `progress_activity` Material Symbol with classes `animate-spin text-[#c0c1ff] text-4xl`.
    2. Create `apps/web/components/ui/EmptyState.tsx`: Accept `title`, `message`, and optional `icon` props. Render a centered div with the icon (Material Symbol, default `text-6xl text-[#908fa0]`), title (`text-lg text-[#e4e1ed]`), and message (`text-sm text-[#908fa0]`).
    3. Create `apps/web/components/ui/ErrorBanner.tsx`: Accept `message` prop. Render an inline div with classes `bg-[#ffb4ab]/10 border border-[#ffb4ab] text-[#ffb4ab] p-3 rounded-lg flex items-center gap-2`. Include an `error` Material Symbol icon.
    4. Edit `apps/web/components/layout/Sidebar.tsx`: Add `cursor-pointer transition-colors duration-200` to all navigation links. Render active links with `border-l-4 border-[#c0c1ff] bg-[#3e495d]/30 text-[#aeb9d0]`. Preserve existing `isActive` logic and pending count badge fetch.
    5. Edit `apps/web/components/layout/TopBar.tsx`: Add a red notification dot (`bg-[#ffb4ab] w-2 h-2 rounded-full absolute top-0 right-0`) to the bell icon container. Apply `focus:ring-1 focus:ring-[#c0c1ff]` to the search input. Ensure the Create Memory button and bell icon have `cursor-pointer`.
  </action>
  <verify>
    <automated>
      - `grep -q "progress_activity" apps/web/components/ui/LoadingSpinner.tsx`
      - `grep -q "bg-\[#ffb4ab\]/10" apps/web/components/ui/ErrorBanner.tsx`
      - `grep -q "cursor-pointer" apps/web/components/layout/Sidebar.tsx`
      - `grep -q "bg-\[#ffb4ab\]" apps/web/components/layout/TopBar.tsx`
      - `grep -q "focus:ring-1 focus:ring-\[#c0c1ff\]" apps/web/components/layout/TopBar.tsx`
    </automated>
  </verify>
  <done>
    - Sidebar Audit: Navigate to the app in browser. Verify all sidebar links show `cursor: pointer` on hover and have a visible background color change (`bg-[#292932]`).
    - TopBar Audit: Verify the bell icon has a visible red dot. Verify the search input shows a focus ring when clicked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → apps/web | External package code crosses into the application |
| Reusable components → page consumers | UI components accept props from parent pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | npm/pip/cargo installs | mitigate | Verify packages are installed from the official npm registry. react-force-graph-2d and @tanstack/react-table are industry-standard packages with high adoption. shadcn/ui is the official CLI from https://ui.shadcn.com. Run `npm audit` after install. |
| T-02-01-02 | Information Disclosure | ErrorBanner component | mitigate | ErrorBanner accepts only a `message` string prop. Implementors must never pass stack traces, internal paths, or API keys to it — only user-safe error strings. |
| T-02-01-03 | Cross-Site Scripting | EmptyState and LoadingSpinner props | mitigate | Icon names are hardcoded Material Symbols strings. Text content is passed as React children (automatically escaped by React). No `dangerouslySetInnerHTML` is used. |
| T-02-01-SC | Tampering | npm/pip/cargo installs | mitigate | Supply chain: install from official registry only. Packages are top-tier with millions of downloads. Treat as [ASSUMED] legitimacy; verify on npmjs.com if any doubt. |
</threat_model>

<verification>
- `cd apps/web && npm run build` passes with zero errors.
- `grep -r "console.error" apps/web/app/page.tsx apps/web/app/graph/page.tsx apps/web/app/review/page.tsx apps/web/app/memories/page.tsx apps/web/app/jobs/page.tsx apps/web/app/settings/page.tsx` returns zero matches (all console.error calls will be removed in subsequent plans).
- All shadcn/ui components exist in `apps/web/components/ui/`.
- All interactive elements in Sidebar and TopBar have `cursor-pointer` classes.
</verification>

<success_criteria>
- react-force-graph-2d and @tanstack/react-table appear in package.json dependencies.
- shadcn/ui components (button, card, dialog, input, label, select, tabs, switch, checkbox, tooltip, badge, table) are present in `apps/web/components/ui/`.
- LoadingSpinner, EmptyState, and ErrorBanner are created and export valid React components.
- Sidebar links have `cursor-pointer` and active link styling (`border-l-4 border-[#c0c1ff]`).
- TopBar bell icon has a red notification dot (`bg-[#ffb4ab]`).
- npm run build completes without errors.
</success_criteria>

<output>
Create `.planning/phases/02-dashboard-redesign/02-01-SUMMARY.md` when done
</output>
