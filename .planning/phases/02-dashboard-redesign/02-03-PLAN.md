---
phase: 02-dashboard-redesign
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/web/app/settings/page.tsx
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, ARCH-02]
must_haves:
  truths:
    - "Settings page uses shadcn/ui Tabs component for 6-tab navigation"
    - "All existing useState hooks (providers, agents, settings) and handler functions are preserved"
    - "Providers tab shows EmptyState when no providers are configured"
  artifacts:
    - path: "apps/web/app/settings/page.tsx"
      provides: "Settings shell with tabs"
      contains: "<Tabs, <TabsList, <TabsTrigger, <TabsContent"
    - path: "apps/web/app/settings/page.tsx"
      provides: "Preserved state logic"
      contains: "const [providers, setProviders]"
  key_links:
    - from: "apps/web/app/settings/page.tsx"
      to: "shadcn/ui Tabs"
      via: "import from components/ui/tabs"
      pattern: "from.*components/ui/tabs"
    - from: "apps/web/app/settings/page.tsx"
      to: "existing provider state"
      via: "preserved useState hooks"
      pattern: "const \[providers, setProviders\]"
---

<objective>
Redesign the Settings page shell (layout, navigation, cards, spacing) while preserving all existing tab content and React state from Phase 1. Migrate to shadcn/ui Tabs and add an empty state for the Providers tab.

Purpose: The settings page has delicate unified React state (Provider Vault -> Agent Routing linkage). The redesign must be cosmetic only — no logic changes.
Output: Settings page with shadcn/ui Tabs, card wrappers, and preserved Phase 1 functionality.
</objective>

<execution_context>
@D:/Projects/Victorious Memory 2V/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Victorious Memory 2V/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-dashboard-redesign/02-CONTEXT.md
@.planning/phases/02-dashboard-redesign/02-RESEARCH.md
@.planning/phases/02-dashboard-redesign/02-UI-SPEC.md
@.planning/phases/02-dashboard-redesign/02-PATTERNS.md
@.planning/phases/02-dashboard-redesign/02-01-SUMMARY.md
@apps/web/AGENTS.md
@apps/web/app/settings/page.tsx
@apps/web/components/ui/LoadingSpinner.tsx
@apps/web/components/ui/EmptyState.tsx
@apps/web/components/ui/ErrorBanner.tsx
@apps/web/components/ui/Tabs.tsx
@apps/web/components/ui/Card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 2-7: Redesign Settings Shell</name>
  <files>apps/web/app/settings/page.tsx</files>
  <action>
    <!-- Covers CONTEXT.md decisions: D-05, D-06, D-07, D-08 (Settings Page) -->
    1. Read `apps/web/AGENTS.md` and consult `node_modules/next/dist/docs/` as mandated before modifying.
    2. Modify `apps/web/app/settings/page.tsx`:
       a. Import shadcn/ui `<Tabs>`, `<TabsList>`, `<TabsTrigger>`, `<TabsContent>` from `components/ui/tabs`.
       b. Wrap the 6 tab panels in `<Tabs defaultValue="providers">`.
       c. Use `<TabsList>` and `<TabsTrigger value="...">` to generate the horizontal tab row with labels: Providers, Extraction, Auto-Approve, Lifecycle, Plugin, Data. Style active tab with `border-b-2 border-[#c0c1ff] text-[#c0c1ff]`.
       d. Use `<TabsContent value="...">` to wrap the existing UI implementations for each tab.
       e. PRESERVE all existing `useState` hooks (`providers`, `agents`, `settings`) and handle functions (`handlePrimaryChange`, `handleFallbackChange`, `handleSaveRouting`). Do NOT modify internal logic of the Agent Routing or Provider Vault sections.
       f. Wrap each tab's content in a shadcn `<Card>` with `bg-[#1f1f27] border border-[#464554] rounded-lg p-6` for visual consistency.
       g. Render `<EmptyState title="No providers configured yet." message="Add a provider from the templates below." icon="cloud_off" />` inside the Providers tab when `providers.length === 0`.
       h. Replace any inline toggle switches with shadcn `<Switch>` components where appropriate, preserving the same `onChange` handlers.
  </action>
  <verify>
    <automated>
      - `grep -q "<Tabs" apps/web/app/settings/page.tsx`
      - `grep -q "<TabsContent" apps/web/app/settings/page.tsx`
      - `grep -q "const handlePrimaryChange" apps/web/app/settings/page.tsx`
      - `grep -q "const handleFallbackChange" apps/web/app/settings/page.tsx`
      - `grep -q "const handleSaveRouting" apps/web/app/settings/page.tsx`
      - `grep -q "<EmptyState" apps/web/app/settings/page.tsx`
      - `grep -q "<Card" apps/web/app/settings/page.tsx`
    </automated>
  </verify>
  <done>
    - Tab Navigation: Open Settings locally. Click through all 6 tabs (Providers, Extraction, Auto-Approve, Lifecycle, Plugin, Data) and confirm instant switching with the correct active tab indicator.
    - State Resilience: In the Providers tab, modify some provider fields (without saving). Switch to the Extraction tab, then back to Providers. Confirm the unsaved changes are still present.
    - Agent Routing: Verify the Provider Vault -> Agent Routing dropdown linkage still works correctly (primary and fallback selects show providers).
    - Empty State: Clear all providers and confirm the "No providers configured yet" empty state renders with the cloud_off icon.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings form state → API | Provider configs and agent routing cross to backend |
| Provider secrets → UI | API keys must never be rendered in error banners or DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | Settings error banners | mitigate | ErrorBanner must never receive raw provider config objects. Only pass sanitized error message strings. Existing provider state handling is preserved unchanged from Phase 1. |
| T-02-03-02 | Tampering | Settings form state via DOM manipulation | accept | No new validation is added in this phase. Existing validation from Phase 1 is preserved. Shell redesign is cosmetic only. |
| T-02-03-03 | Cross-Site Scripting | Provider names or settings values rendered in tabs | mitigate | React default escaping applies. No `dangerouslySetInnerHTML` is used in the settings page. All text is rendered as React children. |
| T-02-03-04 | Information Disclosure | Provider API keys in state | mitigate | API keys are managed by Phase 1 Provider Vault logic. This task only wraps existing UI; it does not introduce new key handling. Keys must not be rendered in plaintext outside password inputs. |
</threat_model>

<verification>
- `cd apps/web && npm run build` passes with zero errors.
- `grep -q "<Tabs" apps/web/app/settings/page.tsx` succeeds.
- `grep -q "const handlePrimaryChange" apps/web/app/settings/page.tsx` succeeds (confirms state preservation).
- `grep -q "const handleFallbackChange" apps/web/app/settings/page.tsx` succeeds.
- `grep -q "const handleSaveRouting" apps/web/app/settings/page.tsx` succeeds.
- No `console.error` calls remain in `apps/web/app/settings/page.tsx`.
</verification>

<success_criteria>
- Settings page uses shadcn/ui `<Tabs>` for 6-tab navigation with active indicator styling.
- All existing `useState` hooks for `providers`, `agents`, and `settings` are preserved unchanged.
- `handlePrimaryChange`, `handleFallbackChange`, and `handleSaveRouting` functions are preserved.
- Provider Vault -> Agent Routing dropdown linkage remains functional.
- Providers tab renders `<EmptyState />` when `providers.length === 0`.
- Each tab's content is wrapped in a shadcn `<Card>` with consistent MD3 dark styling.
- npm run build completes without errors.
</success_criteria>

<output>
Create `.planning/phases/02-dashboard-redesign/02-03-SUMMARY.md` when done
</output>
